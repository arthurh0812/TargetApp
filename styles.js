import { StyleSheet } from "react-native";

export const Colors = StyleSheet.flatten({
  black: "#121212",
  white: "#e8e8e8",
  backgroundDark: "#72063c",
  background: "#84184eff",
  backgroundLight: "#94205aff",
  yellow: "#ddb52f",
  golden: "#b0720e",
  // golden: "#288c70",
  red: "#e11616",
});

export const Fonts = StyleSheet.flatten({
  defaultSize: 20,
  mediumSize: 22,
  largeSize: 26,
  smallSize: 16,
  tinySize: 12,
});
